%% When compiling with LaTeX (i.e., using the dvips graphics driver), 
%% the bounding box cannot be determined by reading the pixel size (apparently).
%% So, for each raster image, we need a .bb file that gives the bounding box
%% of the image. 
%%BoundingBox: 0 0 400 400

